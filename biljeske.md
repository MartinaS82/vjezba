## Šta znamo dosad?

git add . > radni direktorij

git commit -m "sta smo radili" > lokalni repo

git push > globalni repo

git status > stanje projekta, ovako saznajemo da li smo u pravom lokalnom repozitoriju

Problem nastaje kada nas više radi. Tada se služimo granama > kopijama main grane, odnosno to su grane koje imaju istu historiju kao i main grana. 